# AndroidX package structure to make it clearer which packages are bundled with the
# Android operating system, and which are packaged with your app's APK
# https://developer.android.com/topic/libraries/support-library/androidx-rn
android.useAndroidX=true
# Kotlin code style for this project: "official" or "obsolete":
kotlin.code.style=official

# Lint configuration to stabilize CI builds
# Prefer K2 UAST for lint to avoid UastFacade initialization issues
android.lint.useK2Uast=true
android.experimental.lint.useK2Uast=true

# Relax lint to prevent failing the build in CI
android.lint.abortOnError=false
android.lint.checkReleaseBuilds=false
android.lint.checkDependencies=false
