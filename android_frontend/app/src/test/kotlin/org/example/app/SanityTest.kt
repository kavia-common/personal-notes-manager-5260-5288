package org.example.app

import org.junit.Test
import org.junit.Assert.assertTrue

class SanityTest {
    @Test
    fun sanity() {
        // PUBLIC_INTERFACE
        // Simple assertion to ensure the test task always discovers and runs at least one test.
        assertTrue(true)
    }
}
